% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_markers.R
\name{score_markers}
\alias{score_markers}
\title{Score cells against marker gene sets}
\usage{
score_markers(
  expr,
  markers = midbrain_markers,
  method = c("zscore", "ucell", "control", "mean_weighted"),
  n_control = 100,
  n_bins = 25,
  max_rank = 1500,
  seed = NULL
)
}
\arguments{
\item{expr}{A genes x cells numeric matrix (dense or sparse \code{Matrix}) of
normalised (typically log-normalised) expression values. Row names must
be gene symbols.}

\item{markers}{A marker reference data frame with columns \code{cell_type},
\code{gene}, and \code{weight} (see \link{midbrain_markers}). Defaults to the bundled
midbrain reference.}

\item{method}{Scoring method: \code{"zscore"}, \code{"ucell"}, \code{"control"}, or
\code{"mean_weighted"}. See Scoring methods.}

\item{n_control}{Number of control genes sampled per marker gene
(\code{method = "control"} only).}

\item{n_bins}{Number of expression bins used to match control genes to
markers (\code{method = "control"} only).}

\item{max_rank}{Rank cutoff below which genes are treated as tied
(\code{method = "ucell"} only).}

\item{seed}{Optional integer seed. Control gene sampling is random, so
set this for reproducible scores when \code{method = "control"}.}
}
\value{
A cell-types x cells numeric matrix of scores. A row is all \code{NA}
(with a warning) if none of that cell type's markers were found in
\code{expr}.
}
\description{
Computes a per-cell-type score for every cell in an expression matrix,
from a marker gene reference table. This is the scoring engine behind
\code{\link[=annotate_midbrain_cells]{annotate_midbrain_cells()}}; call it directly if you want raw scores
rather than hard-assigned labels.
}
\section{Scoring methods}{

Raw mean expression is a poor basis for comparing scores \emph{across} cell
types: highly-expressed markers (e.g. \code{MBP}) inflate their own set
relative to lowly-expressed ones (e.g. \code{SLC6A3}), and per-cell library
size leaks into the result. The available corrections differ in \emph{what}
they normalise against, which matters more than it might appear — see
\code{vignette("scoring-methods")} for a benchmark.

\describe{
\item{\code{"zscore"} (default)}{Each gene is standardised across cells, then
the weighted mean of the standardised markers is taken. This is the
only method here that compares a gene against \emph{its own distribution
across cells}, which is what detects enrichment relative to a gene's
own baseline. Consequently it is the most robust to markers that
differ in absolute abundance. Its weakness is the flip side: scores
are relative to the cells present, so it degrades on datasets
dominated by a single cell type, and scores are not comparable
between separately-processed batches.}
\item{\code{"ucell"}}{Mann-Whitney U statistic on within-cell gene ranks,
following the UCell formulation. Scores lie in \verb{[0, 1]}, are
independent of the other cells in the matrix (so batches and tiles
are comparable, and cells can be scored in chunks), and are robust to
sparsity. The trade-off: because it ranks \emph{within} a cell, a
constitutively abundant marker set ranks highly in every cell, so it
does not correct for abundance imbalance or ambient/spillover
contamination.}
\item{\code{"control"}}{Background-matched scoring in the style of Seurat's
\code{AddModuleScore}: genes are binned by mean expression, control genes
are drawn from matching bins, and the control mean is subtracted.
Note this is an \emph{additive} correction, so it removes an offset but
not a difference in dynamic range; it helps less than expected when
marker sets differ in abundance. Requires enough non-marker genes in
\code{expr} to build the control pool.}
\item{\code{"mean_weighted"}}{Weighted mean of raw marker expression. Kept
as an uncorrected reference point; not recommended for real use.}
}
}

\examples{
\dontrun{
scores <- score_markers(expr_matrix, seed = 42)
scores[, 1:5]
}
}
