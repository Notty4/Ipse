% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sections.R
\name{list_state_sections}
\alias{list_state_sections}
\title{List the state sections available in a marker reference}
\usage{
list_state_sections(markers = midbrain_markers)
}
\arguments{
\item{markers}{A marker reference data frame. Defaults to the bundled
midbrain reference.}
}
\value{
A data frame with one row per section: \code{context} (the name you
pass to \code{sections}), \code{cell_types} it applies to, \code{n_genes}, and
\code{source}. Zero rows if the reference defines no sections.
}
\description{
State sections are condition-associated signatures (disease states,
reactive states) that are \strong{never scored unless you ask for them} by
name via the \code{sections} argument of \code{\link[=annotate_midbrain_cells]{annotate_midbrain_cells()}}.
}
\details{
The point of a section is interpretability: it gives a named,
reference-anchored cell group that can be compared against published
work, rather than an opaque cluster index that means nothing outside the
run that produced it. The \code{source} column records where each signature
came from, so you can judge whether a given analysis is independent of
the data the signature was derived from.
}
\examples{
list_state_sections()
}
