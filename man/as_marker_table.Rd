% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consensus_markers.R
\name{as_marker_table}
\alias{as_marker_table}
\title{Convert marker candidates into a reference table}
\usage{
as_marker_table(
  candidates,
  n = 6,
  min_gap = 0.1,
  effector = character(0),
  exclude = character(0),
  source = NA_character_
)
}
\arguments{
\item{candidates}{A data frame from \code{\link[=derive_markers]{derive_markers()}} or
\code{\link[=consensus_markers]{consensus_markers()}}.}

\item{n}{Maximum number of genes to keep per cell type.}

\item{min_gap}{Quality floor applied before taking the top \code{n}:
candidates scoring below this on \code{detect_gap} (or \code{specificity} if
detection is unavailable) are discarded. Prevents a cell type with
only two real markers from being padded out with noise, which
degrades annotation rather than improving it. Set \code{NULL} to disable.

The right value is dataset-dependent, not universal: with few cells
per type, background genes reach a detection gap of 0.15 or so by
chance alone, so the default of 0.1 will not clear them. Inspect the
distribution of \code{detect_gap} in your candidates and set the floor
above the noise, rather than trusting the default.}

\item{effector}{Character vector of gene symbols to assign to the
\code{effector} layer instead of \code{identity}.}

\item{exclude}{Character vector of gene symbols to drop entirely,
applied \strong{before} the top \code{n} are taken. This matters: removing
unwanted genes afterwards leaves that cell type with fewer markers
than the others, and a type scored on fewer markers has a noisier
score, which loses it borderline cells to better-populated types.
Excluding first keeps every cell type on a comparable footing.}

\item{source}{A string recording where these markers came from — cohort,
accession, or publication. Strongly recommended.}
}
\value{
A data frame with \code{cell_type}, \code{gene}, \code{layer}, \code{context},
\code{source}, and \code{weight}, ready to pass as \code{markers}.
}
\description{
Takes the output of \code{\link[=derive_markers]{derive_markers()}} or \code{\link[=consensus_markers]{consensus_markers()}} and
produces a table in the format \code{\link[=annotate_cells]{annotate_cells()}} expects.
}
\details{
Layer assignment is deliberately \strong{not} automated. Whether a gene is
lineage-determining (\code{identity}) or part of a functional programme
(\code{effector}) is a biological judgement, and getting it wrong defeats the
purpose of separating them: an effector gene mislabelled as identity
will make cells that have lost that programme unrecognisable. Everything
defaults to \code{identity}; name the effector genes explicitly.
}
\examples{
\dontrun{
markers <- as_marker_table(
  consensus, n = 6, min_gap = 0.2,
  effector = c("TH", "SLC6A3", "SLC18A2", "DDC"),
  source = "Control samples, GSE178265 + GSE140231"
)
annotate_cells(expr, markers = markers)
}
}
