% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_subtypes.R
\name{annotate_subtypes}
\alias{annotate_subtypes}
\title{Sub-label cells of one type using a subtype reference}
\usage{
annotate_subtypes(
  expr,
  labels,
  parent,
  markers = midbrain_da_subtypes,
  method = c("zscore", "ucell", "control", "mean_weighted"),
  min_score = -Inf,
  min_margin = 0,
  seed = NULL
)
}
\arguments{
\item{expr}{A genes x cells matrix, as passed to \code{\link[=annotate_cells]{annotate_cells()}}.}

\item{labels}{The data frame returned by \code{\link[=annotate_cells]{annotate_cells()}}, with one
row per column of \code{expr} in the same order.}

\item{parent}{The \code{cell_type} value to resolve, e.g.
\code{"Dopaminergic neuron"}.}

\item{markers}{A subtype marker reference. Defaults to
\link{midbrain_da_subtypes}.}

\item{method}{Scoring method, passed to \code{\link[=score_markers]{score_markers()}}.}

\item{min_score}{Minimum winning score to accept a subtype call; below
it, cells are \code{"Unassigned"}.}

\item{min_margin}{Minimum gap between best and second-best subtype
score; below it, cells are \code{"Ambiguous"}.}

\item{seed}{Optional integer seed, passed to \code{\link[=score_markers]{score_markers()}}.}
}
\value{
\code{labels} with three columns added: \code{subtype}, \code{subtype_score}
and \code{subtype_margin}. All three are \code{NA} for cells whose \code{cell_type}
is not \code{parent}.
}
\description{
Takes labels from \code{\link[=annotate_cells]{annotate_cells()}} and resolves one broad class into
its subtypes — dopaminergic neurons into the SOX6 and CALB1 populations,
for instance. Cells of other types are left alone.
}
\section{Why this is a separate pass}{

Subtype scores are computed \strong{within the parent class only}. That is
the whole point: a dopaminergic subtype's meaningful competitor is
another dopaminergic subtype, not an oligodendrocyte. Scoring subtypes
alongside broad lineages collapses their scores, because every subtype's
nearest neighbour is a sibling and exclusivity vanishes — the same
reason \link{midbrain_da_subtypes} is derived separately from
\link{midbrain_markers}.

A consequence worth knowing: with composition-dependent methods such as
\code{"zscore"}, the scores here depend on which cells were assigned to the
parent class. Mislabelled cells entering this pass will distort it.
}

\section{Reading the output}{

Expect smaller margins than at the lineage level. Subtypes are
genuinely similar, so a \code{subtype_margin} of 0.05 is not necessarily a
failure — but a population of low-margin cells concentrated in one
condition is a result, not noise. Setting \code{min_margin} marks those
\code{"Ambiguous"} rather than forcing a call.
}

\examples{
\dontrun{
labels <- annotate_cells(expr, seed = 42)
labels <- annotate_subtypes(expr, labels, parent = "Dopaminergic neuron",
                            seed = 42)
table(labels$subtype, useNA = "ifany")

# SOX6_AGTR1 is the population depleted in Parkinson's disease
sum(labels$subtype == "SOX6_AGTR1", na.rm = TRUE)
}
}
