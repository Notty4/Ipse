% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark_methods.R
\name{benchmark_methods}
\alias{benchmark_methods}
\title{Compare scoring methods against known labels}
\usage{
benchmark_methods(
  expr,
  labels,
  markers = midbrain_markers,
  methods = c("zscore", "ucell", "control", "mean_weighted"),
  calibrate = "none",
  seed = 42
)
}
\arguments{
\item{expr}{A genes x cells matrix, as for \code{\link[=annotate_cells]{annotate_cells()}}.}

\item{labels}{Trusted cell type labels, one per column of \code{expr}.}

\item{markers}{A marker reference data frame.}

\item{methods}{Which methods to compare.}

\item{calibrate}{Which calibration settings to compare; every
combination of \code{methods} and \code{calibrate} is evaluated. See
\code{\link[=annotate_cells]{annotate_cells()}}.}

\item{seed}{Passed to \code{\link[=score_markers]{score_markers()}} for reproducibility.}
}
\value{
A data frame with one row per method and calibration setting:
\code{method}, \code{calibrate}, \code{accuracy}
overall, \code{balanced_accuracy} (mean of per-class recall, which does not
let one large class mask failures on small ones), and \code{worst_class}
with its recall. Sorted by balanced accuracy.
}
\description{
Runs every scoring method over the same data and marker set and reports
how well each recovers labels you already trust. Use it whenever you
have annotated data, because which method wins is a property of your
dataset rather than a fact about the methods.
}
\section{Why there is no universal default}{

The methods fail in different circumstances, and those circumstances are
properties of your data:

\itemize{
\item \code{"zscore"} standardises each gene across cells, so it is robust
when marker sets differ in absolute abundance — but it degrades
badly under \strong{class imbalance}. A cell type comprising most of the
dataset pulls up its own markers' means, compressing its members'
scores, while a 1\% type's markers sit near zero for everyone. Weakly
expressing members of the abundant type then fall below the rare
type's baseline and are misassigned to it.
\item \code{"ucell"} scores each cell independently, so composition cannot
distort it — but it cannot correct for a marker set that is
constitutively abundant across all cell types.
\item \code{"control"} and \code{"mean_weighted"} are unaffected by composition
but do not correct abundance imbalance either.
}

Real data is nearly always class-imbalanced, and often also carries
ambient contamination, so both failure modes can be present at once.
Measure rather than assume.
}

\examples{
\dontrun{
benchmark_methods(expr, obj$cell_type, markers = my_markers)
}
}
