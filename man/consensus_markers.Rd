% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consensus_markers.R
\name{consensus_markers}
\alias{consensus_markers}
\title{Combine marker candidates across independent cohorts}
\usage{
consensus_markers(candidate_list, top_n = 50, min_cohorts = NULL)
}
\arguments{
\item{candidate_list}{A named list of data frames as returned by
\code{\link[=derive_markers]{derive_markers()}}. Names are used as cohort labels.}

\item{top_n}{Consider only each cohort's top \code{top_n} candidates per cell
type. Set \code{NULL} to use all candidates that passed \code{min_detect}.}

\item{min_cohorts}{Minimum number of cohorts a gene must appear in to be
retained. Defaults to all of them.}
}
\value{
A data frame with one row per (cell type, gene) surviving the
filter: \code{cell_type}, \code{gene}, \code{n_cohorts}, \code{cohorts} (comma-separated),
\code{mean_specificity}, \code{min_specificity}, and \code{worst_rank} — the poorest
rank it achieved in any cohort, which is the conservative view.
Sorted by cell type then \code{worst_rank}.
}
\description{
A gene that ranks highly in one dataset may be reflecting that dataset's
donors, dissection, or protocol. Requiring a candidate to replicate
across independent cohorts is a stronger filter than any single-dataset
statistic, and is usually worth more than moving to a larger dataset.
}
\examples{
\dontrun{
consensus <- consensus_markers(
  list(kamath = cand_kamath, agarwal = cand_agarwal, siletti = cand_siletti),
  top_n = 50,
  min_cohorts = 2
)
}
}
